<view class="container">
  <block wx:for="{{items}}" wx:key="*this">
    <view class="tabar-item {{index === curIdx ? 'active' : ''}}"
          bindtap="onItemClick"
          mark:idx="{{index}}"
    >{{item}}</view>
  </block>
</view>